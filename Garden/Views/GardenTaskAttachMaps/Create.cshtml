@model Garden.Models.GardenTaskAttachMap

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>GardenTaskAttachMap</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="GardenId" class="control-label"></label>
                <select asp-for="GardenId" class ="form-control" asp-items="ViewBag.GardenId"></select>
            </div>
            <div class="form-group">
                <label asp-for="AttachmentId" class="control-label"></label>
                <select asp-for="AttachmentId" class ="form-control" asp-items="ViewBag.AttachmentId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
